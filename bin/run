#!/bin/bash

# Exit immediately when commands below fail
set -e

# Install dependencies
#
source bin/prepare

# Wait for GitLab service to become available
#
ruby bin/wait

# Run tests
#
xvfb-run bundle exec rspec $@
